#API = Application Program Interface 
# endpoint/route/ Path esta conformada por la URL del proyecto 
# "HTTPS://tuiter.com/API/*endpoint*" 
# CreateReadeUpdateDelete
# CRUD es la base de los endpoints
# se crean los endpoints los cuales son las direcciones donde deberia acceder rapidamente a la informacion
# se crean endpoints para el usuario y la aplicacion
# C = /post     crea un tweet   
# R = /tweets   muestra tweets
# R = /tweets/{tweets_id}    muestra un tweet en especifico
# U = /tweets/{tweet_id}/update      actualiza un tweet
# D = /tweets/{tweet_id}/delete      borra un tweet
# los modelos son las tablas de sql
# endpoint para usuarios
# /users    muestra todos los usuarios
# /singup   registra un usuario
# /users/{user_id}/ muestra un usuario 
# /users/{user_id}/update
# /users/{user_id}/delete  


def main():
    pass


if __name__ == "__main__":
    main